##

install.packages("gapminder") #caught up at 10:30 in class, poor notes - re-read instructions to follow
install.packages("tidyverse")
library(gapminder)
library(tidyverse)


head(gapminder)
tail(gapminder)
str(gapminder)
summary(gapminder)
glimpse(gapminder)

diag(5) #matrix of one data type 

tibble() #dont know what does, breifly wrote 'would see the whole thing exicuted. 
#reg data frame - would be way too long

#always plot the data. 